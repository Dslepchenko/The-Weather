import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    boxWrapper:{
            width:'100%',
            backgroundColor:'#272730',
            borderRadius:'20px',
            minHeight:'400px',
            
    }  
})